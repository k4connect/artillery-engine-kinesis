'use strict';

Object.defineProperty(exports, "__esModule", {
	value: true
});

exports.default = function (parsed, when, value) {
	var negated = when === 'never';
	var closingFullStop = parsed.subject ? parsed.subject[parsed.subject.length - 1] === value : true;
	return [negated ? !closingFullStop : closingFullStop, ['message', negated ? 'may not' : 'must', 'end with full stop'].filter(Boolean).join(' ')];
};

module.exports = exports['default'];