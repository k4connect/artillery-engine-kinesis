'use strict';

Object.defineProperty(exports, "__esModule", {
	value: true
});

var _ensureEnum = require('../library/ensure-enum');

var _ensureEnum2 = _interopRequireDefault(_ensureEnum);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = function (parsed, when, value) {
	var negated = when === 'never';
	var result = (0, _ensureEnum2.default)(parsed.type, value);
	return [negated ? !result : result, [`type must`, negated ? `not` : null, `be one of [${value.map(function (e) {
		return `"${e}"`;
	}).join(', ')}]`].filter(Boolean).join(' ')];
};

module.exports = exports['default'];