'use strict';

Object.defineProperty(exports, "__esModule", {
	value: true
});

exports.default = function (parsed, when) {
	var negated = when === 'never';
	// get complete body split into lines
	var lines = (parsed.raw || '').split('\n').slice(1);
	// check if the first line of body (if any) is empty
	var leadingBlank = lines.length > 0 ? lines[0].length === 0 : true;
	return [negated ? !leadingBlank : leadingBlank, ['body', negated ? 'may not' : 'must', 'have leading blank line'].filter(Boolean).join(' ')];
};

module.exports = exports['default'];