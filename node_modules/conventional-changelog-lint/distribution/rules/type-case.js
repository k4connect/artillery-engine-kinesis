'use strict';

Object.defineProperty(exports, "__esModule", {
	value: true
});

var _ensureCase = require('../library/ensure-case');

var _ensureCase2 = _interopRequireDefault(_ensureCase);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = function (parsed, when, value) {
	var negated = when === 'never';
	var result = (0, _ensureCase2.default)(parsed.type, value);
	return [negated ? !result : result, [`type must`, negated ? `not` : null, `be ${value}`].filter(Boolean).join(' ')];
};

module.exports = exports['default'];