'use strict';

Object.defineProperty(exports, "__esModule", {
	value: true
});
exports.default = resolveExtends;

var _lodash = require('lodash');

// Resolve extend configs
function resolveExtends(config) {
	var prefix = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : '';
	var key = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 'extends';

	return Object.values(config[key] || []).reduce(function (merged, extender) {
		var name = [prefix, extender].filter(String).join('-');
		return (0, _lodash.merge)({}, merged, resolveExtends(require(name)));
	}, config);
}
module.exports = exports['default'];